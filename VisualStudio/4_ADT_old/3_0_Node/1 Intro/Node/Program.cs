using System;
using IntroductionQuestionsSolutions;
using Node;

namespace Node {
	public class Program {
		public static void Main(string[] args) {
		}


			/*
			Student student1 = new Student(15, "Mike");
			Node<Student> myStudents = new Node<Student>(student1);
			Char char1 = 'a';
			Node<Char> myChars = new Node<Char>(char1);
			int int1 = 1;
			Node<Int32> myIntegers = new Node<Int32>(int1);

			myStudents.SetNext(new Node<Student>(new Student(13, "Liza")));
			myChars.SetNext(new Node<Char>('b'));
			myIntegers.SetNext(new Node<Int32>(2));

			Console.WriteLine(myStudents.ToString());
			Console.WriteLine(myChars.ToString());
			Console.WriteLine(myIntegers.ToString());
			*/
	}
}
